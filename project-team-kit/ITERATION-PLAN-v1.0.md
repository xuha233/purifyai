# PurifyAI v1.0 全面迭代计划

**计划版本：** v1.0 迭代路线图
**当前版本：** v0.7.0 RC1
**目标版本：** v1.0.0
**计划制定时间：** 2026-02-24
**预计周期：** 4-6 周

---

## 📋 迭代目标

### 核心目标
1. **UI 全面重构** - 智能体页面布局优化，减少选项卡复杂度
2. **用户体验重构** - 新手引导、交互逻辑优化
3. **性能大幅提升** - Python + Rust 混合架构
4. **前后端联动** - 响应式 UI + 高效后端

### 优先级
```
P0 (必须完成):
├─ 智能体页面 UI 重新设计
├─ 核心计算模块 Rust 加速
├─ 新手引导流程
└─ 选项卡精简

P1 (重要):
├─ 交互逻辑优化
├─ 性能监控和优化
├─ 响应式布局
└─ 错误提示优化

P2 (可选):
├─ 暗色主题
├─ 自定义快捷键
└─ 插件系统
```

---

## 🎨 第一阶段：UI 全面重构 (Week 1-2)

### Issue 1: 智能体页面重新设计

**当前问题：**
- 智能体页面布局混乱
- 信息密度过高
- 可视化效果不清晰
- 操作流程不直观

**设计方案：**

```
┌─────────────────────────────────────────────────────────────────────┐
│ PurifyAI - 智能清理                                                 │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │ 🎯 智能体中心 - 一键智能清理                                   │  │
│  │                                                                │  │
│  │  ┌────────────────────────────────────────────────────────┐   │  │
│  │  │                                                        │   │  │
│  │  │        🔍 扫描    ⚠️ 审查    🗑️ 执行    📊 报告        │   │  │
│  │  │         ↑          ↑          ↑          ↑              │   │  │
│  │  │      等待就绪    等待就绪    等待就绪    等待就绪          │   │  │
│  │  │                                                        │   │  │
│  │  │  [ ███████████████████░░░░░░░      ] 50%              │   │  │
│  │  │      扫描进度: 10万项 / 20万项                          │   │  │
│  │  │                                                        │   │  │
│  │  │  ┌──────────────────────────────────────────────────┐  │   │  │
│  │  │  │ 📌 当前任务: 正在扫描 C:\Windows\Temp           │  │   │  │
│  │  │  │ ⏱️ 预计剩余时间: 5 分钟                           │  │   │  │
│  │  │  └──────────────────────────────────────────────────┘  │   │  │
│  │  │                                                        │   │  │
│  │  │              [ 开始清理 ]  [ 暂停 ]  [ 取消 ]         │   │  │
│  │  │                                                        │   │  │
│  │  └────────────────────────────────────────────────────────┘   │  │
│  │                                                                │  │
│  │  ┌────────────────────────────────────────────────────────┐   │  │
│  │  │ 💡 智能体工作台                                          │   │  │
│  │  │                                                        │   │  │
│  │  │  🔧 工具调用: 42 次    $ 成本: $2.34   ✅ 成功率: 95%   │   │  │
│  │  │                                                        │   │  │
│  │  │  📊 风险分析:                                          │   │  │
│  │  │  ├─ 高风险: 125 项  🚨                                │   │  │
│  │  │  ├─ 中风险: 452 项  ⚠️                                │   │  │
│  │  │  └─ 低风险: 1,234 项  ✅                              │   │  │
│  │  │                                                        │   │  │
│  │  │  🎯 AI 建议:                                          │   │  │
│  │  │  "发现 125 个高风险系统文件，建议谨慎清理"             │   │  │
│  │  │                                                        │   │  │
│  │  │              [ 查看详细 ]  [ 手动管理 ]                │   │  │
│  │  │                                                        │   │  │
│  │  └────────────────────────────────────────────────────────┘   │  │
│  │                                                                │  │
│  └──────────────────────────────────────────────────────────────┘  │
│                                                                     │
├─────────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ⚙️  设置  ❓ 帮助  ⏱️ 历史                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────────┘
```

**关键改进：**
- ✅ 单页面布局，减少切换
- ✅ 流程可视化更直观（4 阶段横向展示）
- ✅ 实时进度和状态一目了然
- ✅ 智能体工作台信息聚合
- ✅ 操作按钮清晰明确

**实现要点：**
- 使用 `GridLayout` 重新组织布局
- 动态进度条和状态指示器
- 实时更新工具调用和成本信息
- AI 建议卡片组件化

---

### Issue 2: 选项卡精简

**当前问题：**
- 选项卡太多（可能 7+ 个）
- 用户不知道选哪个
- 功能逻辑重叠

**精简方案：**

| 当前选项卡 | 状态 | v1.0 方案 | 原因 |
|-----------|------|-----------|------|
| 智能体中心 | 保留 | ✅ 保留 | **主页面**，核心功能 |
| 智能清理 | 合并 | ↘️ 合并到智能体中心 | 功能重复 |
| 手动清理 | 保留 | ✅ 保留作为专家模式 | 高级用户需要 |
| 定时清理 | 保留 | ✅ 整合为"定时任务" | 独立功能模块 |
| 报告历史 | 保留 | ✅ 精简为历史 | 去重 |
| 设置 | 保留 | ✅ 保留 | 必要 |
| 帮助 | 合并 | ↘️ 整合为"指南" | 整合文档 |

**v1.0 最终选项卡（精简到 4 个）：**

```
┌─────────────────────────────────────────────────────────────┐
│ 🏠 首页        🧹 清理        🕒 定时        ⚙️ 设置         │
└─────────────────────────────────────────────────────────────┘
```

**说明：**
- **首页（智能体中心）** - 一键智能清理，新手引导
- **清理** - 手动清理 + 高级设置（合并智能清理到首页）
- **定时** - 定时清理任务
- **设置** - 应用设置

**实现要点：**
- 重构 `QTabWidget` 结构
- 合并重复功能页面
- 导航栏图标化
- 底部固定导航（移动端友好）

---

### Issue 3: 新手引导流程

**当前问题：**
- 新用户不知道怎么用
- 功能入口不清晰
- 缺少使用提示

**引导流程设计：**

```
┌─────────────────────────────────────────────────────────────────┐
│ 👋 欢迎使用 PurifyAI                                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   💭 PurifyAI 是智能磁盘清理工具，帮您安全高效地                 │
│      清理计算机垃圾文件、临时文件和重复文件。                     │
│                                                                  │
│   ⚡ 核心功能：                                                   │
│   • 一键智能清理（自动分析 + 安全清理）                         │
│   • 手动精细控制（高阶用户选项）                                 │
│   • 定时自动清理（后台运行）                                     │
│   • 清理报告追踪（可回溯历史）                                   │
│                                                                  │
│   [ 下一步 ]  [ 跳过引导 ]                                       │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ 🚀 快速开始指南                                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   📖 步骤 1/3: 选择清理区域                                      │
│                                                                  │
│   ☑ C:\Windows\Temp    临时文件                                 │
│   ☑ C:\Users\(用户名)\AppData\Local  应用缓存                   │
│   ☐ 自定义磁盘          ...                                      │
│                                                                  │
│   [ 选择全部 ]  [ 选择推荐区域 ]  [ 下一步 ]                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ 🧹 首次清理                                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   准备开始第一次清理！                                            │
│   点击"开始清理"按钮，AI 会：                                     │
│   • 扫描检测垃圾文件                                              │
│   • 智能分析文件风险                                              │
│   • 安全清理可删除项                                              │
│   • 生成清理报告                                                  │
│                                                                  │
│   ⏱️ 预计时间: 5-10 分钟                                        │
│                                                                  │
│   [ 开始清理 ]  [ 取消 ]                                         │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ ✅ 清理完成                                                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│   🎉 恭喜！首次清理完成                                          │
│   • 清理文件: 1,245 项                                            │
│   • 释放空间: 2.3 GB                                              │
│   • 风险文件: 5 项（已跳过）                                      │
│                                                                  │
│   💡 提示：                                                       │
│   • 查看清理报告可了解详细信息                                    │
│   • 定期清理可保持系统流畅                                        │
│   • 设置定时清理可自动运行                                        │
│                                                                  │
│   [ 查看报告 ]  [ 完成引导 ]                                     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

**实现要点：**
- 创建 `OnboardingWizard` 类
- 步骤化引导流程（4 步）
- 记录用户选择（保存到配置）
- 引导完成后标记已完成（避免重复）

---

### Issue 4: 交互逻辑优化

**当前问题：**
- 操作流程不直观
- 确认框太多
- 状态反馈不及时
- 错误处理不友好

**优化方案：**

#### 4.1 减少确认框

**当前流程（繁琐）：**
```
点击清理 → 确认清理 → 确认删除每个文件 → 再次确认 → 完成
```

**优化后流程（简洁）：**
```
点击清理 → 显示预览 → 一键清理（带进度） → 完成 → 可撤销
```

#### 4.2 实时状态反馈

**实现方案：**
```python
# 实时进度更新
def on_progress_update(percent, current_status):
    progress_bar.setValue(percent)
    status_label.setText(current_status)

    # 关键步骤添加 toast 通知
    if percent == 25:
        show_toast("📊 扫描完成，开始智能分析...")
    elif percent == 50:
        show_toast("⚠️ 发现 125 个高风险文件")
    elif percent == 100:
        show_toast("✅ 清理完成，释放 2.3 GB 空间")
```

#### 4.3 错误提示优化

**方案：**
- 使用 `InfoBar`（qfluentwidgets）替代 `QMessageBox`
- 错误信息可操作（一键重试/跳过）
- 错误日志一键复制

```python
# 友好的错误提示
def show_error_with_actions(error, actions):
    info_bar = InfoBar.error(
        title="清理失败",
        content=f"无法删除文件: {error.path}",
        orient=Qt.Horizontal,
        isClosable=True,
        position=InfoBarPosition.TOP,
        duration=-1,  # 不自动关闭
        parent=self
    )

    # 添加操作按钮
    for action in actions:
        info_bar.addWidget(action)

    info_bar.show()
```

---

**第一阶段工作量估算：**
```
智能体页面的 UI 重新设计 (3 天)
选项卡精简 (1 天)
新手引导流程 (2 天)
交互逻辑优化 (2 天)
总计: 8 个工作日 (~1.5 周)
```

---

## ⚡ 第二阶段：性能大幅提升 (Week 3-4)

### Issue 5: 核心模块 Rust 加速

**当前性能问题：**
- 文件扫描 20 万项耗时 ~15 秒
- 规则匹配 1 万项耗时 ~8 秒
- AI 分析 1 千项 ~10 秒（API 额外延迟）
- 清理操作 1 万项 ~12 秒

**性能目标：**
- 文件扫描 20 万项 → **<5 秒** (提速 3x)
- 规则匹配 1 万项 → **<2 秒** (提速 4x)
- AI 分析 1 千项 → **<8 秒** (提速 1.25x)
- 清理操作 1 万项 → **<4 秒** (提速 3x)

### 技术方案：Python + Rust 混合架构

```
┌─────────────────────────────────────────────────────────────────┐
│ PurifyAI v1.0 架构                                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  PyQT5 UI Layer (Python)                                         │
│  ├─ agent_hub_page.py       ← UI 渲染                           │
│  ├─ smart_cleanup_page.py  ← 用户交互                           │
│  └─ dashboard.py           ← 主框架                             │
│           │                                                      │
│           ▼                                                      │
│  Business Logic Layer (Python + Rust)                            │
│  ├─ scanner.py      →  Rust 扫描引擎 (PyO3)                      │
│  ├─ rule_engine.py  →  Rust 规则引擎 (PyO3)                      │
│  ├─ ai_analyzer.py  →  Python (API 调用)                        │
│  └─ cleaner.py      →  Rust 清理引擎 (PyO3)                      │
│           │                                                      │
│           ▼                                                      │
│  Rust Native Modules (高性能)                                  │
│  ├─ purifyai-scanner/core.rs   ← 文件系统扫描                    │
│  ├─ purifyai-rules/core.rs     ← 快速规则匹配                    │
│  └─ purifyai-cleaner/core.rs   ← 安全删除操作                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 详细实现

#### 5.1 Rust 扫描引擎

**文件结构：**
```
purifyai-scanner/
├─ Cargo.toml
├─ src/
│  ├─ lib.rs           # PyO3 接口
│  ├─ scanner.rs       # 核心扫描逻辑
│  ├─ walker.rs        # 并行文件遍历
│  └─ filter.rs        # 文件过滤
└─ pyproject.toml      # Python 打包配置
```

**核心代码（scanner.rs）：**
```rust
use pyo3::prelude::*;
use walkdir::WalkDir;
use rayon::prelude::*;

#[pyclass]
pub struct Scanner {
    max_files: usize,
    exclude_patterns: Vec<String>,
}

#[pymethods]
impl Scanner {
    #[new]
    pub fn new(max_files: usize) -> Self {
        Scanner {
            max_files,
            exclude_patterns: Vec::new(),
        }
    }

    pub fn scan_directory(&self, path: &str) -> PyResult<Vec<ScanItem>> {
        let mut items = Vec::new();

        // 并行遍历文件系统
        WalkDir::new(path)
            .into_iter()
            .par_bridge()  // Rayon 并行
            .filter_map(|e| e.ok())
            .take(self.max_files)
            .for_each(|entry| {
                if let Ok(metadata) = entry.metadata() {
                    if metadata.is_file() {
                        items.push(ScanItem {
                            path: entry.path().to_string_lossy().to_string(),
                            size: metadata.len(),
                            modified: metadata.modified()
                                .ok()
                                .map(|t| t.duration_since(std::time::UNIX_EPOCH).unwrap().as_secs()),
                        });
                    }
                }
            });

        Ok(items)
    }
}

#[pyclass]
#[derive(Clone)]
pub struct ScanItem {
    #[pyo3(get, set)]
    pub path: String,
    #[pyo3(get, set)]
    pub size: u64,
    #[pyo3(get, set)]
    pub modified: Option<u64>,
}
```

**PyO3 接口（lib.rs）：**
```rust
use pyo3::prelude::*;

mod scanner;

#[pymodule]
fn purifyai_scanner(_py: Python, m: &PyModule) -> PyResult<()> {
    m.add_class::<scanner::Scanner>()?;
    m.add_class::<scanner::ScanItem>()?;
    Ok(())
}
```

**Python 封装（scanner_wrapper.py）：**
```python
from purifyai_scanner import Scanner, ScanItem

def fast_scan(path: str, max_files: int = 100_000):
    """快速扫描目录（使用 Rust 加速）"""
    scanner = Scanner(max_files)
    return scanner.scan_directory(path)

# 性能对比
if __name__ == '__main__':
    import time
    path = "C:\\Windows\\Temp"

    # Python 原版
    start = time.time()
    items = scan_directory_python(path)  # 15 秒
    print(f"Python: {len(items)} items in {time.time() - start:.2f}s")

    # Rust 加速版
    start = time.time()
    items = fast_scan(path)  # 5 秒
    print(f"Rust: {len(items)} items in {time.time() - start:.2f}s")
```

#### 5.2 Rust 规则引擎

**核心优化：**
- 使用 `regex` crate 快速匹配
- 预编译正则表达式
- 批量评分（SIMD 优化）

```rust
use regex::Regex;
use rayon::prelude::*;

#[pyclass]
pub struct RuleEngine {
    patterns: Vec<(Regex, i32)>,  // (正则, 分数)
}

#[pymethods]
impl RuleEngine {
    pub fn rate_batch(&self, items: Vec<ScanItem>) -> Vec<i32> {
        items
            .par_iter()  // 并行评分
            .map(|item| self.rate_item(item))
            .collect()
    }

    fn rate_item(&self, item: &ScanItem) -> i32 {
        let mut score = 0;

        for (pattern, points) in &self.patterns {
            if pattern.is_match(&item.path) {
                score += points;
            }
        }

        score
    }
}
```

#### 5.3 Rust 清理引擎

**安全删除：**
- 使用 `_delete_file_secure()` 确保文件被彻底删除
- 支持撤销（先移动到备份目录）
- 原子操作（确保完整性）

```rust
use std::fs;

#[pyclass]
pub struct Cleaner {
    backup_dir: String,
}

#[pymethods]
impl Cleaner {
    pub fn delete_secure(&self, path: &str, backup: bool) -> PyResult<()> {
        if backup {
            // 先备份
            let backup_path = format!("{}/{}", self.backup_dir,
                                      path.replace(":", "_").replace("\\", "/"));
            fs::copy(path, &backup_path)?;
        }

        // 安全删除
        fs::remove_file(path)?;
        Ok(())
    }

    pub fn undo_delete(&self, backup_path: &str, original_path: &str) -> PyResult<()> {
        fs::copy(backup_path, original_path)?;
        Ok(())
    }
}
```

### PyO3 打包和分发

**Cargo.toml：**
```toml
[package]
name = "purifyai-scanner"
version = "0.1.0"
edition = "2021"

[lib]
name = "purifyai_scanner"
crate-type = ["cdylib"]

[dependencies]
pyo3 = { version = "0.20", features = ["extension-module"] }
walkdir = { version = "2.4", features = ["rayon"] }
rayon = "1.8"
regex = "1.10"

[features]
extension-module = ["pyo3/extension-module"]
default = ["extension-module"]
```

**构建命令：**
```bash
# 安装 maturin（Python-Rust 打包工具）
pip install maturin

# 构建 wheel 包
cd purifyai-scanner
maturin develop --release

# 会生成一个 wheel 包
# 并自动安装到当前 Python 环境
```

**requirements.txt 添加：**
```
# Rust 加速模块
purifyai-scanner==0.1.0  # 发布到 PyPI
purifyai-rules==0.1.0
purifyai-cleaner==0.1.0
```

---

### Issue 6: 性能监控

**实现方案：**
- 使用 `cProfile` 监控 Python 函数
- 使用 `perf` 监控 Rust 函数
- 可视化性能报告（火焰图）

```python
# perf_monitor.py
import cProfile
import pstats
import time
from functools import wraps

def monitor_performance(func):
    @wraps(func)
    def wrapper(*args, **kwargs):
        profiler = cProfile.Profile()
        profiler.enable()

        start = time.time()
        result = func(*args, **kwargs)
        elapsed = time.time() - start

        profiler.disable()
        stats = pstats.Stats(profiler)
        stats.sort_stats('cumtime')
        stats.print_stats(10)  # 打印最慢的 10 个函数

        print(f"\n⏱️ {func.__name__} took {elapsed:.2f}s")

        return result
    return wrapper

# 使用
@monitor_performance
def scan_directory(path: str):
    # 扫描逻辑
    ...
```

---

**第二阶段工作量估算：**
```
Rust 扫描引擎实现 (4 天)
Rust 规则引擎实现 (3 天)
Rust 清理引擎实现 (2 天)
PyO3 集成和测试 (3 天)
性能监控和优化 (2 天)
总计: 14 个工作日 (~2.5 周)
```

---

## 🚀 第三阶段：前后端联动 (Week 5-6)

### Issue 7: 响应式 UI

**当前问题：**
- 大操作时 UI 卡顿
- 进度更新不流畅
- 操作延迟感强

**解决方案：**
- 使用 `QThread` 异步处理
- 使用 `pyqtSignal` 跨线程通信
- 使用 `QTimer` 延迟加载

```python
# async_task.py
from PyQt5.QtCore import QThread, pyqtSignal

class ScanWorker(QThread):
    progress_updated = pyqtSignal(int, str)
    completed = pyqtSignal(list)

    def __init__(self, path: str):
        super().__init__()
        self.path = path

    def run(self):
        """后台执行扫描"""
        from purifyai_scanner import Scanner
        scanner = Scanner(max_files=100_000)

        # 分批次发送进度
        items = []
        for i, item in enumerate(scanner.scan_directory(self.path)):
            items.append(item)

            # 每扫描 1000 项发送一次进度
            if i % 1000 == 0:
                progress = min(i * 100 // 100_000, 100)
                self.progress_updated.emit(progress, f"扫描中: {i} 项")

        self.completed.emit(items)

# 在 UI 中使用
worker = ScanWorker("C:\\Windows\\Temp")
worker.progress_updated.connect(self.on_progress)
worker.completed.connect(self.on_completed)
worker.start()
```

---

### Issue 8: 后端任务队列

**实现方案：**
- 使用 `concurrent.futures.ThreadPoolExecutor`
- 任务队列管理（优先级队列）
- 任务状态可视化

```python
# task_queue.py
from concurrent.futures import ThreadPoolExecutor, as_completed
from queue import PriorityQueue
import threading

class Task:
    def __init__(self, priority: int, func, *args, **kwargs):
        self.priority = priority
        self.func = func
        self.args = args
        self.kwargs = kwargs
        self.id = id(self)

    def __lt__(self, other):
        return self.priority < other.priority

class TaskQueue:
    def __init__(self, max_workers: int = 4):
        self.queue = PriorityQueue()
        self.executor = ThreadPoolExecutor(max_workers=max_workers)
        self.tasks = {}
        self.lock = threading.Lock()

    def submit(self, task: Task) -> str:
        """提交任务到队列"""
        self.queue.put(task)

        future = self.executor.submit(task.func, *task.args, **task.kwargs)

        with self.lock:
            self.tasks[task.id] = {
                'task': task,
                'future': future,
                'status': 'running'
            }

        return task.id

    def get_status(self, task_id: str) -> dict:
        """获取任务状态"""
        with self.lock:
            return self.tasks.get(task_id, {})

    def cancel(self, task_id: str) -> bool:
        """取消任务"""
        with self.lock:
            if task_id in self.tasks:
                self.tasks[task_id]['future'].cancel()
                return True
        return False
```

---

### Issue 9: 实时通知系统

**实现方案：**
- 使用 `QSystemTrayIcon` 显示系统通知
- Toast 通知（右下角弹出）
- 任务完成通知

```python
# notification.py
from PyQt5.QtWidgets import QSystemTrayIcon, QMenu, QAction
from PyQt5.QtGui import QIcon

class NotificationCenter:
    def __init__(self):
        self.tray_icon = QSystemTrayIcon()
        self.tray_icon.setIcon(QIcon('app_icon.png'))

        # 创建菜单
        menu = QMenu()
        show_action = QAction("显示", self)
        quit_action = QAction("退出", self)
        menu.addAction(show_action)
        menu.addAction(quit_action)

        self.tray_icon.setContextMenu(menu)
        self.tray_icon.show()

    def show_notification(self, title: str, message: str, duration: int = 3000):
        """显示系统通知"""
        self.tray_icon.showMessage(
            title,
            message,
            QSystemTrayIcon.Information,
            duration
        )
```

---

**第三阶段工作量估算：**
```
异步任务处理 (2 天)
后端任务队列 (2 天)
实时通知系统 (1 天)
前后端集成测试 (2 天)
总计: 7 个工作日 (~1.5 周)
```

---

## 📊 各阶段工作量汇总

| 阶段 | 工作内容 | 预计天数 | 状态 |
|------|----------|----------|------|
| Week 1-2 | UI 全面重构 | 8 | ⬜ 未开始 |
| Week 3-4 | 性能大幅提升 | 14 | ⬜ 未开始 |
| Week 5-6 | 前后端联动 | 7 | ⬜ 未开始 |
| **总计** | | **29 天** | |

---

## 🎯 里程碑

| 里程碑 | 日期 | 交付物 |
|--------|------|--------|
| M1: UI 重构完成 | Week 2 结束 | 新版 UI 上线 |
| M2: 性能优化完成 | Week 4 结束 | Rust 模块集成 |
| M3: 联动优化完成 | Week 6 结束 | 版本 v1.0.0 发布 |

---

## 📦 技术栈升级

| 组件 | 当前版本 | v1.0 版本 | 说明 |
|------|----------|----------|------|
| 语言 | Python | Python + Rust | 混合架构 |
| GUI | PyQt5 | PyQt5 | 不变 |
| 打包 | PyInstaller | PyInstaller + Maturin | 支持 Rust |
| 并发 | threading | threading + rayon | Rust 并行 |
| 性能监控 | - | cProfile + perf | 新增 |

---

## 📝 下一步行动

### 立即开始（今天）
1. ✅ 创建迭代计划文档
2. ⬜ 创建任务分支 `feature/v1.0-refactor`
3. ⬜ 开始第一阶段：UI 全面重构

### 本周
1. ⬜ 智能体页面 UI 重新设计
2. ⬜ 选项卡精简
3. ⬜ 新手引导流程设计

---

**计划文档创建时间：** 2026-02-24 12:15
**计划执行预计开始：** 2026-02-25
**预计 v1.0.0 发布：** 2026-04-15
